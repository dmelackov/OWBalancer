[project]
name = "shared-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=4.3.0",
    "sqlalchemy>=2.0.41",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["balancer_models"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
